apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-pv-claim
  namespace: docker-registry
spec:
{% if volume_labels %}
  selector:
    matchLabels: {{ volume_labels }}
{% endif %}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi